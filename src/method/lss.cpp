#include "method.h"

namespace layout{
    py::object generate_lss_within_plan (py::dict _room, pyBox _region, py::dict history, string const &type, py::dict params, py::dict info){
        py::dict ret;
        return ret;
    }
}
